{
  "name": "number",
  "version": "1.0.0",
  "description": "Custom form field for Kissflow's form",
  "main": "index.js",
  "scripts": {
    "dev": "form-field-scripts dev",
    "build": "form-field-scripts build",
    "zip": "rm -rf ./number.zip ./dist && npm run build && cd ./dist && zip -q -r ../number.zip . && rm -rf ../dist",
    "unzip": "rm -rf ./number && unzip number.zip -d ./number"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@kissflow/form-field-config": "workspace:^",
    "@kissflow/form-field-scripts": "workspace:^",
    "unzipper": "^0.10.14",
    "zip": "^1.2.0"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ],
    "rules": {}
  },
  "keywords": [
    "lcnc",
    "no-code",
    "low-code",
    "platform",
    "react",
    "cli",
    "kissflow",
    "custom-components",
    "custom-fields"
  ],
  "dependencies": {
    "helpers": "workspace:^",
    "primereact": "^10.8.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
